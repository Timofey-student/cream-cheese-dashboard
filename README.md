# Cream Cheese Dashboard

Streamlit dashboard for cream cheese category analysis based on loyal Violette cohort.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py

---

## 6. Создай репозиторий на GitHub
На GitHub:
- нажми **New repository**
- название, например: `cream-cheese-dashboard`
- Public
- `Add README` не ставь, если делаешь всё локально сам
- нажми **Create repository**

---

## 7. Залей проект через Terminal
В папке проекта выполни:

```bash
git init
git add .
git commit -m "Initial dashboard app"
git branch -M main
git remote add origin https://github.com/ТВОЙ_ЛОГИН/cream-cheese-dashboard.git
git push -u origin main